- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim:
#    ~/.vimrc:
     ~/.zshrc:
     ~/.gitconfig:
    #  may want to think about adding the whole config directory here
     ~/.config/bat:
        create: true
        path: bat

- create:
    - ~/Work

- shell:
   - 
     command: ./setup_homebrew.zsh
     stdout: true
     stderr: true
